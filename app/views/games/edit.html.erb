<h1 style = "text-align: center;"> <%=  @game.name %></h1>


<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
     <%= simple_form_for(@game) do |f|%>
        <%= f.input :name %>
        <%= f.input :category, :collection => ["Video games", "Consoles", "Boardgames", "Peripherals"] %>
        <%= f.input :location %>
        <%= f.input :price_per_day %>
        <%= f.input :description %>
        <div class="row justify-content-center">
          <%= f.submit class: 'btn btn-danger text-white' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
